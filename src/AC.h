#pragma once
// Header for the Averages Calculation file

#include <iostream>
#include <string>
#include <vector>
#include <numeric>

int ACmain();
void ACGetNumericValue(std::vector<int> &values);
void ACGetDigitValue(std::vector<int> &values);